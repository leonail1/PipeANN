# Label Sidecar Layout Audit

Supersector32K repacking copies the existing `_labels.densebit` sidecar and preserves `label_size=0` in the main disk-index metadata. Node records therefore still contain vector and graph payload only.
